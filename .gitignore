node_modules/
dist/
build/
coverage/
*.log

# Python venv for the CocoIndex code-index sidecar (see sidecar/requirements.txt)
.venv/
__pycache__/

# env / secrets
.env
.env.*
!.env.example
# Anchored to the repo root so it ignores a top-level secrets/ dir WITHOUT swallowing the
# `src/secrets/` source directory (crypto/vault/validator live there).
/secrets/
*.pem
*.key

# OS / editor
.DS_Store
.idea/
*.swp

# Claude Code local overrides (keep shared .claude/, ignore personal)
.claude/settings.local.json
